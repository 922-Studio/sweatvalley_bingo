---
phase: 04-deploy-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/server.js
  - client/src/App.js
  - .github/workflows/deploy.yml
autonomous: true
requirements: [CICD-02, CICD-03, CICD-04, CICD-05, CICD-06, ACCS-02, ACCS-03, ACCS-04]

must_haves:
  truths:
    - "Server CORS is locked to production domain and localhost dev origin"
    - "Socket.io uses WebSocket-only transport on both server and client"
    - "Socket.io ping interval is 10s with 5s timeout to survive Cloudflare idle timeout"
    - "Client connects to window.location.origin in production (no hardcoded port)"
    - "GitHub Actions workflow file exists with test, version-bump, deploy, and Discord notify steps"
    - "Workflow uses concurrency group that cancels previous in-progress runs"
  artifacts:
    - path: "server/server.js"
      provides: "Production CORS, Socket.io WebSocket-only transport, aggressive ping settings"
      contains: "bingo.922-studio.com"
    - path: "client/src/App.js"
      provides: "Production Socket.io URL using window.location.origin, WebSocket-only transport"
      contains: "window.location.origin"
    - path: ".github/workflows/deploy.yml"
      provides: "Full CI/CD pipeline: test, version bump, docker compose deploy, Discord notify"
      contains: "deploy-$"
  key_links:
    - from: "client/src/App.js"
      to: "server/server.js"
      via: "Socket.io connection with matching transport config"
      pattern: "transports.*websocket"
    - from: ".github/workflows/deploy.yml"
      to: "docker-compose.yml"
      via: "docker compose up -d --build step"
      pattern: "docker compose up"
---

<objective>
Production-harden server and client Socket.io/CORS configuration and create the GitHub Actions CI/CD workflow file.

Purpose: All code and configuration changes needed for production deployment -- CORS lockdown, WebSocket-only transport, aggressive ping settings for Cloudflare compatibility, and the full CI/CD pipeline definition.
Output: Modified server.js and App.js with production settings, plus .github/workflows/deploy.yml ready for use once the repo is created.
</objective>

<execution_context>
@/Users/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-go-live/04-RESEARCH.md

@server/server.js
@client/src/App.js
@docker-compose.yml

<interfaces>
<!-- Current server.js Socket.io config (lines 38-44) that needs replacing: -->
```javascript
const io = socketIO(httpServer, {
  cors: {
    origin: "*",
    methods: ["GET", "POST"],
    transports: ['websocket', 'polling']
  }
});

app.use(cors());
```

<!-- Current client App.js Socket.io connection (lines 25-36) that needs replacing: -->
```javascript
const socketURL = process.env.NODE_ENV === 'production'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://localhost:3001';

const newSocket = io(socketURL, {
  reconnectionDelay: 1000,
  reconnection: true,
  reconnectionAttempts: 10,
  transports: ['websocket', 'polling'],
  agent: false,
  upgrade: false,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production-harden server CORS and Socket.io config</name>
  <files>server/server.js</files>
  <action>
Modify the Socket.io initialization in server/server.js `createServer` function:

1. Replace `cors: { origin: "*" }` with explicit origin whitelist:
   ```javascript
   cors: {
     origin: [
       "https://bingo.922-studio.com",
       "http://localhost:3000"
     ],
     methods: ["GET", "POST"]
   }
   ```

2. Add WebSocket-only transport and aggressive ping settings to the Socket.io options:
   ```javascript
   transports: ['websocket'],
   pingInterval: 10000,
   pingTimeout: 5000
   ```
   These go INSIDE the socketIO() options object (same level as cors), NOT inside cors.

3. Replace `app.use(cors())` with explicit origin whitelist:
   ```javascript
   app.use(cors({
     origin: [
       "https://bingo.922-studio.com",
       "http://localhost:3000"
     ]
   }));
   ```

Do NOT change any other part of server.js. The health endpoint, game logic, and module exports must remain identical.
  </action>
  <verify>
    <automated>cd /Users/gregor/dev/Schweisstal_bingo/server && npm test</automated>
  </verify>
  <done>server.js has CORS locked to bingo.922-studio.com + localhost:3000, Socket.io uses WebSocket-only transport with pingInterval 10000 and pingTimeout 5000. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Production-harden client Socket.io connection</name>
  <files>client/src/App.js</files>
  <action>
Modify the Socket.io connection in client/src/App.js useEffect (around line 25):

1. Replace the socketURL logic:
   ```javascript
   const socketURL = process.env.NODE_ENV === 'production'
     ? window.location.origin
     : 'http://localhost:3001';
   ```
   In production, use `window.location.origin` (no port) since Cloudflare Tunnel routes to the server which serves both static files and Socket.io on the same origin.

2. Replace the io() options:
   ```javascript
   const newSocket = io(socketURL, {
     reconnectionDelay: 1000,
     reconnection: true,
     reconnectionAttempts: 10,
     transports: ['websocket'],
   });
   ```
   Remove `agent: false` and `upgrade: false` (not needed with WebSocket-only).
   Remove `'polling'` from transports array.

Do NOT change any other part of App.js. All event handlers, state management, and rendering must remain identical.
  </action>
  <verify>
    <automated>cd /Users/gregor/dev/Schweisstal_bingo/client && CI=true npx react-scripts test --watchAll=false 2>&1 | tail -20</automated>
  </verify>
  <done>App.js uses window.location.origin for production Socket.io URL and WebSocket-only transport. Client tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` with the following structure:

```yaml
name: Deploy Schweisstal Bingo

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install server dependencies
        run: cd server && npm install

      - name: Run server tests
        run: cd server && npm test

      - name: Install client dependencies
        run: cd client && npm install

      - name: Run client tests
        run: cd client && npx react-scripts test --watchAll=false
        env:
          CI: true

      - name: Version bump
        uses: phips28/gh-action-bump-version@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          minor-wording: 'feat'
          major-wording: 'BREAKING CHANGE'
          patch-wording: 'fix,chore,refactor,docs,style,perf'

      - name: Deploy
        run: docker compose up -d --build

      - name: Health check
        run: |
          sleep 10
          curl -f http://localhost:3923/health || exit 1

      - name: Discord notification
        if: always()
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "Schweisstal Bingo Deploy"
          description: "Deployment to bingo.922-studio.com"
```

Key details:
- Single job (self-hosted runner IS the deploy server, no artifact transfer needed)
- `concurrency` with `cancel-in-progress: true` for CICD-06
- Version bump uses `phips28/gh-action-bump-version` which adds `[skip ci]` to avoid infinite loops
- Health check uses curl against localhost:3923 (the host port mapping)
- Discord notification uses `if: always()` to fire on both success and failure
- Use `docker compose` (v2, no hyphen) per research recommendations
  </action>
  <verify>
    <automated>test -f /Users/gregor/dev/Schweisstal_bingo/.github/workflows/deploy.yml && echo "Workflow file exists" && grep -q "concurrency" /Users/gregor/dev/Schweisstal_bingo/.github/workflows/deploy.yml && echo "Has concurrency" && grep -q "discord" /Users/gregor/dev/Schweisstal_bingo/.github/workflows/deploy.yml && echo "Has Discord" && grep -q "docker compose" /Users/gregor/dev/Schweisstal_bingo/.github/workflows/deploy.yml && echo "Has deploy step"</automated>
  </verify>
  <done>deploy.yml exists with all required steps: checkout, server tests, client tests, version bump, docker compose deploy, health check, Discord notification. Concurrency group configured with cancel-in-progress.</done>
</task>

</tasks>

<verification>
- `cd server && npm test` passes (CORS/Socket.io changes don't break game logic tests)
- `cd client && CI=true npx react-scripts test --watchAll=false` passes (Socket.io URL change doesn't break component tests)
- `.github/workflows/deploy.yml` exists with correct YAML structure
- `grep "bingo.922-studio.com" server/server.js` returns matches (CORS locked)
- `grep "window.location.origin" client/src/App.js` returns match (production URL)
- `grep "transports.*websocket" server/server.js client/src/App.js` returns matches in both files
</verification>

<success_criteria>
- Server CORS locked to bingo.922-studio.com + localhost:3000
- Socket.io WebSocket-only transport on both server and client
- Socket.io pingInterval 10000, pingTimeout 5000 on server
- Client uses window.location.origin in production
- GitHub Actions workflow file complete with all 7 steps
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-go-live/04-01-SUMMARY.md`
</output>
