---
phase: 03-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/src/setupTests.js
  - client/src/App.test.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "npx react-scripts test --watchAll=false in client/ passes all tests"
    - "App component renders the welcome screen with BINGO title"
    - "App component renders name input, create button, and join button"
    - "socket.io-client is properly mocked so tests run without a server"
  artifacts:
    - path: "client/src/App.test.js"
      provides: "React component tests for App (TEST-03)"
      min_lines: 30
    - path: "client/src/setupTests.js"
      provides: "Jest test setup with jest-dom matchers"
      min_lines: 1
  key_links:
    - from: "client/src/App.test.js"
      to: "client/src/App.js"
      via: "import and render"
      pattern: "import App from './App'"
    - from: "client/src/App.test.js"
      to: "socket.io-client"
      via: "jest.mock"
      pattern: "jest\\.mock\\('socket\\.io-client'"
---

<objective>
Add client-side React component tests using Jest via react-scripts and @testing-library/react, verifying that the App component renders correctly with socket.io-client mocked.

Purpose: TEST-03 requires client React components to be tested with Jest. The App component is the sole component and needs its socket dependency mocked to render in isolation.
Output: App.test.js with passing tests, setupTests.js for jest-dom matchers.
</objective>

<execution_context>
@/Users/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-suite/03-RESEARCH.md

@client/src/App.js
@client/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install testing libraries, create setupTests.js, write App component tests</name>
  <files>client/package.json, client/src/setupTests.js, client/src/App.test.js</files>
  <behavior>
    - App renders welcome screen with "BINGO" in the title
    - App renders a name input with placeholder "Gib deinen Namen ein"
    - App renders "Spiel erstellen" (create game) button
    - App renders "Beitreten" (join) button
    - App renders grid size buttons (3x3 and 4x4)
    - App renders rounds input field
  </behavior>
  <action>
    1. Install testing libraries: `cd client && npm install --save-dev @testing-library/react @testing-library/jest-dom @testing-library/user-event`
       NOTE: Use @testing-library/react@^14 (not v16) for compatibility with react-scripts 5.0.1 which bundles Jest 27. Research flagged this as an open question -- v14 is the safe choice.
    2. Create `client/src/setupTests.js`:
       ```js
       import '@testing-library/jest-dom';
       ```
       This file is auto-detected by react-scripts and adds jest-dom matchers (toBeInTheDocument, etc.)
    3. Write `client/src/App.test.js`:
       - Mock socket.io-client BEFORE importing App:
         ```js
         const mockSocket = {
           on: jest.fn(),
           emit: jest.fn(),
           close: jest.fn(),
           id: 'test-socket-id',
         };
         jest.mock('socket.io-client', () => ({
           io: jest.fn(() => mockSocket),
         }));
         ```
       - Import React, render from @testing-library/react, App.
       - Test: renders "BINGO" text (use regex /BINGO/ since it has emoji around it).
       - Test: renders name input with placeholder containing "Gib deinen Namen ein".
       - Test: renders "Spiel erstellen" button.
       - Test: renders "Beitreten" button.
       - Test: renders grid size options "3x3" and "4x4".
       - Test: renders rounds input (type="number").
       - NOTE: The App component calls `io()` on mount via useEffect. The mock prevents real connections. If the mock setup causes issues, ensure mockSocket.on returns the mock itself for chaining, or adjust mock to satisfy the component's expectations.
    4. Run: `cd client && npx react-scripts test --watchAll=false` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /Users/gregor/dev/Schweisstal_bingo/client && npx react-scripts test --watchAll=false 2>&1</automated>
  </verify>
  <done>@testing-library packages installed, setupTests.js created, App.test.js has 6+ tests for welcome screen rendering, all pass with react-scripts test</done>
</task>

</tasks>

<verification>
Run client test suite:
```bash
cd /Users/gregor/dev/Schweisstal_bingo/client && npx react-scripts test --watchAll=false
```
All App component tests pass.
</verification>

<success_criteria>
- @testing-library/react (v14), @testing-library/jest-dom, @testing-library/user-event installed in client devDependencies
- setupTests.js imports jest-dom for extended matchers
- App.test.js mocks socket.io-client and tests welcome screen rendering (title, input, buttons, grid options)
- `cd client && npx react-scripts test --watchAll=false` exits 0 with all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-suite/03-02-SUMMARY.md`
</output>
