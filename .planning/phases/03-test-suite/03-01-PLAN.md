---
phase: 03-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/gameLogic.js
  - server/server.js
  - server/package.json
  - server/vitest.config.js
  - server/gameLogic.test.js
  - server/socket.test.js
  - server/integration.test.js
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]
must_haves:
  truths:
    - "npx vitest run in server/ passes all tests with zero failures"
    - "generateGrid, checkForLines, createPlayerGrid are importable from gameLogic.js"
    - "server.js still works identically when run directly (require.main === module guard)"
    - "Socket.io events (create-game, join-game, mark-word, start-game) behave correctly under test"
    - "A real Socket.io client can connect, create a game, join, start, and mark words end-to-end"
  artifacts:
    - path: "server/gameLogic.js"
      provides: "Extracted pure game logic functions"
      exports: ["generateGrid", "checkForLines", "createPlayerGrid"]
    - path: "server/server.js"
      provides: "Refactored server with factory pattern"
      exports: ["createServer", "loadWords"]
    - path: "server/gameLogic.test.js"
      provides: "Unit tests for game logic (TEST-01)"
      min_lines: 50
    - path: "server/socket.test.js"
      provides: "Socket.io event handler tests (TEST-02)"
      min_lines: 40
    - path: "server/integration.test.js"
      provides: "End-to-end Socket.io integration test (TEST-04)"
      min_lines: 40
    - path: "server/vitest.config.js"
      provides: "Vitest configuration for CommonJS server"
      min_lines: 5
  key_links:
    - from: "server/server.js"
      to: "server/gameLogic.js"
      via: "require('./gameLogic')"
      pattern: "require\\('./gameLogic'\\)"
    - from: "server/gameLogic.test.js"
      to: "server/gameLogic.js"
      via: "require('./gameLogic')"
      pattern: "require\\('./gameLogic'\\)"
    - from: "server/integration.test.js"
      to: "server/server.js"
      via: "require('./server') createServer factory"
      pattern: "createServer"
---

<objective>
Refactor server.js to extract testable game logic, then add comprehensive server-side tests covering game logic unit tests (TEST-01), Socket.io event handler tests (TEST-02), and real client-server integration tests (TEST-04).

Purpose: The server currently has all logic in a single file with functions trapped inside callbacks and module-level side effects. Extracting and testing makes the game logic verifiable and prevents regressions.
Output: gameLogic.js (pure functions), refactored server.js (factory pattern), vitest config, and 3 test files all passing.
</objective>

<execution_context>
@/Users/gregor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gregor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-suite/03-RESEARCH.md
@.planning/phases/03-test-suite/03-VALIDATION.md

@server/server.js
@server/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract gameLogic.js, refactor server.js to factory pattern, write game logic unit tests</name>
  <files>server/gameLogic.js, server/server.js, server/package.json, server/vitest.config.js, server/gameLogic.test.js</files>
  <behavior>
    - checkForLines: horizontal line in 4x4 (mark indices 0-3) returns 1
    - checkForLines: diagonal in 3x3 (mark 0,4,8) returns 1
    - checkForLines: no complete line returns 0
    - checkForLines: all marked in 3x3 returns 8 (3 rows + 3 cols + 2 diags)
    - generateGrid: returns 16 words for 4x4
    - generateGrid: returns 9 words for 3x3
    - generateGrid: does not mutate input array
    - createPlayerGrid: creates correct 2D array from flat word list
    - server.js still starts normally when run directly (manual sanity -- tested by Docker)
  </behavior>
  <action>
    1. Install vitest: `cd server && npm install --save-dev vitest`
    2. Create `server/vitest.config.js`:
       ```js
       const { defineConfig } = require('vitest/config');
       module.exports = defineConfig({ test: { globals: true, testTimeout: 10000 } });
       ```
    3. Add test script to server/package.json: `"test": "vitest run --reporter=verbose"`
    4. Create `server/gameLogic.js`: Extract `generateGrid`, `checkForLines`, `createPlayerGrid` from server.js. These are pure functions -- copy them verbatim and add `module.exports = { generateGrid, checkForLines, createPlayerGrid }`. The `checkForLines` function is currently INSIDE the `io.on('connection')` callback (line 207 of server.js) -- it must be moved out.
    5. Refactor `server/server.js`:
       - Import from gameLogic.js: `const { generateGrid, checkForLines, createPlayerGrid } = require('./gameLogic');`
       - Remove the three extracted function definitions (generateGrid at line 59, createPlayerGrid at line 106, checkForLines at line 207)
       - Wrap the entire server setup (Express app, http server, Socket.io, routes, socket handlers) in a `createServer(wordsList)` function that accepts words as a parameter instead of using the module-level `words` variable. The function returns `{ app, httpServer, io }`.
       - Keep `loadWords()` as a standalone exported function but change it to RETURN the words array instead of assigning to module-level `words`.
       - Add `require.main === module` guard: only call loadWords() and httpServer.listen() when run directly.
       - Add `module.exports = { createServer, loadWords }` at the bottom.
       - Keep `createGame` function inside createServer (it uses the `games` Map which should be per-server-instance).
       - IMPORTANT: The health check endpoint, static file serving, SPA catch-all, and ALL socket event handlers must remain inside createServer and work identically.
    6. Write `server/gameLogic.test.js` with the behaviors listed above using `const { describe, it, expect } = require('vitest')` and `const { generateGrid, checkForLines, createPlayerGrid } = require('./gameLogic')`.
    7. Run `cd server && npx vitest run gameLogic.test.js --reporter=verbose` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /Users/gregor/dev/Schweisstal_bingo/server && npx vitest run gameLogic.test.js --reporter=verbose</automated>
  </verify>
  <done>gameLogic.js exports 3 pure functions, server.js uses factory pattern with require.main guard, vitest configured, all gameLogic unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Socket.io event tests and full integration test</name>
  <files>server/socket.test.js, server/integration.test.js</files>
  <behavior>
    - create-game: emits game-created with gameId string
    - join-game: emits player-joined with players array containing the joiner
    - join-game with invalid gameId: emits error with message
    - start-game: emits game-started with playerGrids and gridSize
    - mark-word: emits player-marked with score
    - Integration: host creates game, player joins, host starts, player marks word -- full flow works end-to-end with real connections
  </behavior>
  <action>
    1. Write `server/socket.test.js` (TEST-02): Test Socket.io event handlers using the createServer factory from the refactored server.js.
       - Import: `const { createServer, loadWords } = require('./server')` and `const ioc = require('socket.io-client')`
       - In `beforeAll`: call `loadWords()` to get words, call `createServer(words)` to get `{ httpServer, io }`, listen on port 0 (random), create client socket with `ioc('http://localhost:${port}')`, wait for 'connect' event.
       - In `afterAll`: disconnect client, close io, close httpServer.
       - Use a `waitFor(socket, event)` helper that returns a Promise with a 5-second timeout.
       - Tests for: create-game (returns gameId), join-game (returns players), join-game with bad ID (returns error), start-game (returns playerGrids + gridSize), mark-word (returns player-marked with score).
       - For start-game test: must create game first, then start it. For mark-word: must create, start, then mark.
       - IMPORTANT: Use `httpServer.listen(0)` for random port to avoid EADDRINUSE conflicts.
    2. Write `server/integration.test.js` (TEST-04): Full end-to-end flow with TWO real socket clients (host + player).
       - Same setup pattern but create TWO client sockets.
       - Single test: host creates game -> player joins (both receive player-joined) -> host starts game (both receive game-started with their grids) -> player marks a word (receives player-marked with updated score).
       - Verify the full chain works with real network connections.
       - IMPORTANT: Clean up both sockets and server in afterAll.
    3. Run `cd server && npx vitest run --reporter=verbose` -- ALL server tests (gameLogic + socket + integration) must pass.
  </action>
  <verify>
    <automated>cd /Users/gregor/dev/Schweisstal_bingo/server && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>socket.test.js tests all 4 required events (create-game, join-game, mark-word, start-game), integration.test.js verifies full game flow with real Socket.io connections, all server tests pass</done>
</task>

</tasks>

<verification>
Run full server test suite:
```bash
cd /Users/gregor/dev/Schweisstal_bingo/server && npx vitest run --reporter=verbose
```
All tests across gameLogic.test.js, socket.test.js, and integration.test.js must pass.

Verify server still starts normally:
```bash
cd /Users/gregor/dev/Schweisstal_bingo && docker compose up -d && sleep 3 && curl -s http://localhost:3923/health && docker compose down
```
</verification>

<success_criteria>
- gameLogic.js exports generateGrid, checkForLines, createPlayerGrid as pure testable functions
- server.js exports createServer factory and loadWords, only auto-starts with require.main guard
- vitest installed and configured in server/package.json
- gameLogic.test.js: 8+ tests covering checkForLines (horizontal, vertical, diagonal, none, multiple), generateGrid (4x4 count, 3x3 count, no mutation), createPlayerGrid
- socket.test.js: tests for create-game, join-game, mark-word, start-game events
- integration.test.js: full game flow test with 2 real Socket.io clients
- `cd server && npx vitest run` exits 0 with all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-suite/03-01-SUMMARY.md`
</output>
